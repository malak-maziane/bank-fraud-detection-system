FROM python:3.12-slim

WORKDIR /app

RUN python -m pip install --no-cache-dir fastapi uvicorn[standard] scikit-learn pandas

COPY main.py /app/main.py

EXPOSE 8000
CMD ["uvicorn", "main:app", "--host", "0.0.0.0", "--port", "8000"]
